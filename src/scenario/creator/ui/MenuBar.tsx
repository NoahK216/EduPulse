import { Panel } from "@xyflow/react";
import { tabs } from "../../nodes";

const MenuBar = ({ scenarioUrl }: { scenarioUrl?: string }) => {
    return (
        <div className="min-w-full min-h-10 bg-gray-100 text-gray-900 p-3">
           EduPulse
        </div>
    );

}

export default MenuBar;
