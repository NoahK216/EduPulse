import { sectionClassName, sectionHeaderClassName } from "./tabStyles";

export function StartTab() {
  return (
    <section className={sectionClassName}>
      <p className={sectionHeaderClassName}>Start Node</p>
    </section>
  );
}
